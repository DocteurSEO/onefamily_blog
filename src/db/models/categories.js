export default {
  CREATE_TABLE: `CREATE TABLE IF NOT EXISTS categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR 
    ) 

`,
};

// INSERT INTO categories(name) VALUES (seo);
// INSERT INTO categories(name) VALUES (marketing);
// INSERT INTO categories(name) VALUES (wordpress);
